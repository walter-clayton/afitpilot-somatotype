{
  "name": "afitpilot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start:dev-v1 --prefix ./server\" \"npm run start --prefix ./client\"",
    "build": "concurrently \"npm run build-v1 --prefix ./server\" \"npm run build --prefix ./client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Walter Clayton",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
